vector<ll>d(n+5,mod);
    vector<bool>vis(n+5,false);
    d[x] = 0;
    multiset<pair<int,int>>s;
    s.insert({0,x});
    while(s.size()){
     auto it =s.begin();
     ll q = it->S;
     if(q == y){break;}
      s.erase(it);
     if(vis[q]){continue;}
     vis[q] = true;
     for(auto it:v[q]){
      if(d[q] + it.S < d[it.F]){
        d[it.F] = d[q] + it.S;
        s.insert({d[it.F],it.F});
      }
     }
    }